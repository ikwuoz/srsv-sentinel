{
  "name": "srsv-sentinel",
  "version": "0.1.0",
  "description": "Autonomous net-flow sentinel for Standard Reserve: F_n = buys - sells, signal_n = F_{n-1}+F_{n-2}",
  "type": "module",
  "bin": {
    "sentinel": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && npm run build:web-math",
    "build:web-math": "tsc src/math.ts --outDir web/vendor --module es2022 --target es2022 --moduleResolution node --skipLibCheck",
    "start": "node dist/cli.js status",
    "watch": "node dist/cli.js watch",
    "serve": "node dist/server.js",
    "test": "vitest run",
    "demo": "npm run build && node dist/cli.js demo"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "dotenv": "^17.4.2",
    "hono": "^4.7.0",
    "viem": "^2.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
